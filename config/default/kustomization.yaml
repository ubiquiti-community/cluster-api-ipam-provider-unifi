apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ipam-system

namePrefix: unifi-ipam-

# Labels to add to all resources and selectors
commonLabels:
  app.kubernetes.io/name: cluster-api-ipam-provider-unifi
  app.kubernetes.io/component: controller

resources:
  - ../crd
  - ../rbac
  - ../manager
  # [WEBHOOK] Uncomment to enable webhook resources (requires --enable-webhook flag in manager)
  # - ../webhook

# Patches
patches:
  # Protect the /metrics endpoint by putting it behind auth.
  # If you want your controller-manager to expose the /metrics
  # endpoint w/o any authn/z, please comment the following line.
  - path: manager_auth_proxy_patch.yaml

  # [WEBHOOK] Uncomment to enable webhook patches (requires cert-manager and --enable-webhook flag)
  # - path: manager_webhook_patch.yaml
  # - path: webhookcainjection_patch.yaml
# [WEBHOOK] Uncomment to enable webhook certificate injection
# replacements:
#   - source:
#       kind: Service
#       version: v1
#       name: webhook-service
#       fieldPath: .metadata.name
#     targets:
#       - select:
#           kind: MutatingWebhookConfiguration
#         fieldPaths:
#           - .webhooks.*.clientConfig.service.name
#       - select:
#           kind: ValidatingWebhookConfiguration
#         fieldPaths:
#           - .webhooks.*.clientConfig.service.name
#   - source:
#       kind: Service
#       version: v1
#       name: webhook-service
#       fieldPath: .metadata.namespace
#     targets:
#       - select:
#           kind: MutatingWebhookConfiguration
#         fieldPaths:
#           - .webhooks.*.clientConfig.service.namespace
#       - select:
#           kind: ValidatingWebhookConfiguration
#         fieldPaths:
#           - .webhooks.*.clientConfig.service.namespace
